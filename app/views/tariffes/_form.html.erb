<%= form_for(@tariffe) do |f| %>
  <% if @tariffe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tariffe.errors.count, "error") %> prohibited this tariffe from being saved:</h2>

      <ul>
      <% @tariffe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
