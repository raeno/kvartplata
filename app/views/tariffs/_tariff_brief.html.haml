.tariff
  -if tariff == current_tariff
    %h4 текущий тариф ( c #{l(tariff.start_date.to_date, :format => :long)} )
  -else
    %h4 c #{l(tariff.start_date.to_date, :format => :long)} по #{l(tariff.end_date.to_date, :format => :long)}
  %div{:class => 'tariff-body'}
    .names.span3
      %p горячая вода
      %p холодная вода
      %p водоотведение
      %p электроэнергия
    .values
      %p #{tariff.hot_water} р
      %p #{tariff.cold_water} р
      %p #{tariff.utilities} р
      %p #{tariff.energy} р